server:
  port: 8080
spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:reservationdb
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect    